export default function PrivateFolder(){
    return(
        <h1>
            This page we can't see on browser because of uderscore(_)
            <br/>
            If we want to see _folder name the in front of folder name type %5F it will decode as uderscore(_)
        </h1>
    )
}